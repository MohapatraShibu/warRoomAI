# ollama configuration (free, local LLM — https://ollama.com)
# the system works WITHOUT Ollama using rule-based fallback logic.
# if Ollama is running, set the model you have pulled (eg llama3.2, mistral, gemma3)

OLLAMA_BASE_URL=
OLLAMA_MODEL=
